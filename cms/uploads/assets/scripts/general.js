/*
 * Como estamos usando jQuery como framework JavaScript,
 * lo normal sería usar este lenguaje para el manejo
 * de nuestros elementos del DOM.
 *
 * Por eso podéis observar que empiezo el fichero con la
 * llamada principal de jQuery en la cual detectamos que
 * la página ha finalizado su carga, para aplicar nuestras
 * funciones y llamadas.
 *
 */
$(document).ready( function() {

});